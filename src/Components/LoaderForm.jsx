import React from "react";

const LoaderForm = ({isLoading, error}) => {
  return (
    <div id="loader_form">
      <div className="circle-side-2"></div>
    </div>
  );
};

export default LoaderForm;